# Compiled output
/dist
/tmp
/out-tsc
/bazel-out

# Node
/node_modules
npm-debug.log
yarn-error.log

# IDEs and editors
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# Miscellaneous
/.angular/cache
.sass-cache/
/connect.lock
/coverage
/libpeerconnection.log
testem.log
/typings
__screenshots__/

# System files
.DS_Store
Thumbs.db

# Environment files (keep templates)
src/environments/environment.prod.ts

# Local credential notes — must never be committed
CREDENTIALS.md

# Playwright MCP scratch output (snapshots, console logs, screenshots)
.playwright-mcp/

# ⚠️ Capturas de pantalla sueltas EN LA RAÍZ. Existe porque ya pasó: el 26 de
# septiembre de 2026 siete capturas de una revisión de la web acabaron dentro de
# un commit —1,4 MB— porque se hicieron en la raíz y un `git add -A` se las
# llevó. Y el repositorio es público.
#
# Una herramienta que hace capturas las deja donde esté el directorio de
# trabajo, no en `.playwright-mcp/`, así que ignorar esa carpeta no basta. El
# patrón cubre los nombres que salen de una revisión (`mob-*`, `flota-*`,
# `captura-*`) sin tocar los iconos de `src/assets/` ni los de `public/`, que
# son del proyecto y sí van al repositorio.
/*.png
/*.jpg
/*.jpeg
/*.webp

# Herramientas locales, no del proyecto
.claude/
.agents/
skills-lock.json
.playwright-mcp/

# Material criptográfico. NUNCA en el repositorio.
#
# El certificado de firma vive en Secret Manager (`VELTO_SIGNING_CERT`, el .p12
# en base64). Cualquier copia local es de trabajo y se borra: quien tenga el
# fichero y su contraseña puede firmar contratos en nombre de la empresa.
#
# El 4 de septiembre de 2026 aparecieron un .p12 y su base64 sueltos en
# `public/`. No llegaron a commitearse ni a publicarse —hosting sirve `dist/`—
# pero un `git add .` los habría subido.
*.p12
*.pfx
*.key
cert.b64

# Salidas de trabajo que no son código y estaban versionadas sin motivo:
# el log de un despliegue concreto (563 KB) y tres PDF de prueba del contrato
# (~3,4 MB entre los tres). Se regeneran solos y su historia no le sirve a
# nadie; en el repositorio solo engordan cada clon.
deploy.log
# Lo deja el CLI de Firebase al fallar un despliegue. Pasó con la cuota de
# CPU de Cloud Run el 19 de septiembre de 2026.
firebase-debug.log
test-contract-*.pdf

# El kit del Velto Design System tal cual lo entrega Claude Design: 1,2 MB de
# los que casi todo son las Gotham y los SVG del logo en base64 dentro de un
# bundle autoextraíble. Dorel lo guarda aparte, así que aquí no hace falta.
#
# Lo que SÍ se versiona es lo que se lee: `velto-design-system.css` —la hoja
# extraída, 7,8 KB— y el README que dice qué se llevó a la aplicación y qué no.
# Sin ellos, la sección «Tema y color» de CLAUDE.md apuntaría a una carpeta
# vacía.
docs/design/*.html
.firebase/
