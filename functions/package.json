{
  "name": "functions",
  "description": "Cloud Functions for Velto - Redsys payments, contract PDF generation, signing links, and Resend email",
  "scripts": {
    "lint": "echo \"Skipping lint\"",
    "build:tsc": "tsc",
    "build:fonts": "node scripts/copy-fonts.js",
    "build": "npm run build:tsc && npm run build:fonts",
    "test": "vitest run",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "logs:dev": "firebase functions:log --project dev",
    "logs:prod": "firebase functions:log --project prod"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@signpdf/placeholder-pdf-lib": "^3.3.0",
    "@signpdf/signer-p12": "^3.3.0",
    "@signpdf/signpdf": "^3.3.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "fontkit": "1.9.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@types/fontkit": "^2.0.9",
    "typescript": "^5.5.0",
    "vitest": "^4.1.11"
  },
  "private": true
}
